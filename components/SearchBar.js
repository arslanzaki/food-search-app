import React from "react";
import { Text, View } from "react-native";

const SearchBar = () => {
  return (
    <View className="bg-gray-200 h-12 rounded-lg mx-4">
      <Text className="font-bold text-center">SearchBar</Text>
    </View>
  );
};

export default SearchBar;
